[package]
name = "riff_tests"
version = "0.1.0"
edition = "2024"
publish = false

[dependencies]
riff = { path = "../riff" }
tokio = { version = "1", features = ["rt", "rt-multi-thread", "time", "macros"] }

[lints]
workspace = true
